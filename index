void main(){
 /*String name = 'kboy';
 //final a = "변하지 않는 변수";
 //var b = "variable";




 //변수 타입 확인
 print(age.runtimeType);
 print(isTrue.runtimeType);


 //변수의 길이 측정
 print(f.length);


 //줄바꿈 코드
 String h = "Flutter 대학은\n재미있다";
 print(h);


 //String i = '''Flutter
 대학은
 재미있다''';*/




 int a = 1;
 //int b = 2;
 int c = -1;


 double d = 0.5;
 bool isLogin = true;
 bool b = a == 1;
 print(b);


 if(b){ print ('a는 1이다'); }


 //배열
 //가변 길이 리스트
 List<String> languages = ["Dart", "Java", "Python", "C++", "Java"];
 print(languages);
 List<int> numbers = [1, 2, 3, 4];
 print(numbers);
 List<bool> bools = [true, true, false, false];
 print(bools);


 print(bools.runtimeType);
 print(bools[0].runtimeType);


 print(languages.length);
 print(numbers.length);
 print(languages[0].length); //bool이랑 number은 길이측정x




 //고정 길이 리스트
 var fixedList1 = List<int>.filled(5, 0); //길이가 5, 0으로 초기화
 print(fixedList1);
 final fixedList2 = List<int>.filled(6, 0);
 print(fixedList1);


 var growableList3 = List<int>.from([1, 2, 3, 4], growable: true);  //가변길이: 길이를 바꿀 수 있다(true일때)
 print(growableList3);




 //리스트 원소 추가
 growableList3.add(9);
 print(growableList3);


 //리스트 원소 여러개 추가
 numbers.addAll([6, 7, 8]);
 print(numbers);


 //해당하는 값의 원소 제거
 print(languages);
 languages.remove("Java");
 print(languages);


 //인덱스로 해당 원소 제거
 languages.removeAt(0);
 print(languages);


 //배열에 사용할 수 있는 메서드
 // 1. forEach()
 // Dart에서 중갈호를 사용하는 익명함수의 형태: 매개변수를 받아서 중관로 안의 코드를 그것에 대해 실행하겠다.
 (languages) {print(languages); };


 //forEach() 메서드는 리스트를 돌며 원소를 하나씩 꺼낸다
 languages.forEach((languages) { print(languages); });


 // 2. map
 // languages의 각 문자열을 그대로 다시 꺼내서 새로운 문자열 리스트인 languageTexts를 만들어보자
 List<String> languageText = languages.map( (languages) => languages).toList();  //익명함수의 한 형태: 매개변수를 받아서 오른쪽 표현식의 결과로 변환하겠다는 의미
 print(languageText);


 List<String> languageText2 = languages.map( (languages) => '언어: $languages').toList();
 print(languageText2);




} //end of main

